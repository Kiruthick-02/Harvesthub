# Created by Gemini for a React (CRA) project.

# =====================================================================
#  DEPENDENCIES - The most important rule!
#  This folder is huge and can be regenerated by running `npm install`.
# =====================================================================
/node_modules

# =====================================================================
#  BUILD FOLDER - This is for the production version of your app.
#  It's generated automatically when you run `npm run build`.
#  Do not commit generated code to your repository.
# =====================================================================
/build

# =====================================================================
#  ENVIRONMENT VARIABLES - CRITICAL FOR SECURITY!
#  These files contain secret API keys and other sensitive data.
#  NEVER commit these to a public or shared repository.
# =====================================================================
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# You SHOULD commit an example file so other developers know what
# variables are needed. The '!' negates the rule above for this specific file.
!/.env.example

# =====================================================================
#  LOGS - These are for debugging on your local machine only.
# =====================================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =====================================================================
#  IDE / EDITOR CONFIGURATION - Personal settings for your editor.
# =====================================================================
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# =====================================================================
#  TESTING - Test reports and coverage folders.
# =====================================================================
/coverage

# =====================================================================
#  OPERATING SYSTEM FILES - Clutter from macOS, Windows, etc.
# =====================================================================
.DS_Store
Thumbs.db